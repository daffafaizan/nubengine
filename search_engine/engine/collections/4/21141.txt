HLOOKUP adalah fungsi yang sama persis, tetapi mencari data yang telah diformat oleh baris, bukan kolom. Format fungsi VLOOKUP adalah: VLOOKUP(lookup_value,table_array,col_index_num,range_lookup). Lookup_value adalah input pengguna. Ini adalah nilai yang digunakan fungsi untuk mencari.LOOKUP adalah fungsi yang sama persis, tetapi mencari data yang telah diformat oleh baris, bukan kolom. Format fungsi VLOOKUP adalah: VLOOKUP(lookup_value,table_array,col_index_num,range_lookup). Lookup_value adalah input pengguna. Ini adalah nilai yang digunakan fungsi untuk mencari.