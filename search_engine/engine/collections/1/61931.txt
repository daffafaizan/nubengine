<!DOCTYPE html> adalah doctype untuk dokumen. Ini pada dasarnya memberi tahu browser web cara merender halaman (ada doctypes yang berbeda). Jika halaman dirender dengan tidak benar dengan doctype yang benar, itu berarti halaman tersebut tidak dikodekan dengan benar.