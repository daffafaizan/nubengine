Cukup ketik: Namun, jika Anda ingin menjalankannya secara otomatis saat terminal dimulai alih-alih menjalankannya setiap kali Anda membuka terminal, Anda dapat menambahkan. ~/.bash_profile ke file ~/.bashrc. Catatan: Saat Anda membuka terminal, terminal memulai bash dalam mode interaktif (non-login), yang berarti akan sumber ~/.bashrc.~/.bash_profile hanya bersumber dari bash ketika dimulai dalam mode login interaktif. Itu biasanya hanya ketika Anda login di konsol (. +. +. .. ), atau terhubung melalui ssh.ote: Saat Anda membuka terminal, terminal memulai bash dalam mode interaktif (non-login), yang berarti akan sumber ~/ .bashrc. ~/.bash_profile hanya bersumber dari bash ketika dimulai dalam mode login interaktif. Itu biasanya hanya ketika Anda login di konsol (. +. +. .. ), atau terhubung melalui ssh.